Task: Create the initial FastAPI backend scaffold for Education Center CRM.

Context:
This project is an Education Center CRM with MVP-first scope. The documented architecture already exists in the project docs. Follow:

* docs/project_brief_final.md
* docs/mvp_scope_final.md
* docs/domain_model_final.md
* docs/api_surface_plan.md
* docs/backend_scaffold_plan.md
* docs/coding_rules.md

Goal of this task:
Create the first working backend skeleton so that the app can start successfully and expose a basic health-style root response plus a versioned API base placeholder.

Scope of this task:
Create only the initial backend scaffold, not the full system.

Create or update these files if missing:

* backend/app/main.py
* backend/app/api/**init**.py
* backend/app/api/routes/**init**.py

If needed, also create minimal package marker files:

* backend/app/**init**.py

Requirements:

1. Create a FastAPI app instance.
2. Set a clear title for the API, such as "Education Center CRM API".
3. Add a simple root endpoint:

   * GET /
   * returns a small JSON payload with app name and status
4. Add a versioned API router placeholder:

   * prefix: /api/v1
5. Keep structure ready for future route modules.
6. Do not implement database code yet in this task.
7. Do not implement auth yet in this task.
8. Keep code clean, minimal, and readable.

Technical expectations:

* main.py should contain app bootstrap only.
* Use APIRouter properly.
* Add short docstrings or comments only where useful.
* Avoid unnecessary complexity.
* Keep the file small.

Acceptance criteria:

* The backend can be started with uvicorn.
* Visiting / returns JSON.
* The project structure matches the documented backend scaffold direction.
* The code is consistent with docs/coding_rules.md.

After implementation:

1. Try to run the app.
2. Report:

   * files created
   * files changed
   * commands run
   * result
   * any follow-up recommendation

Important:
This task is only the first scaffold step. Do not jump ahead into models, schemas, auth, or database unless strictly necessary to make the app start.
